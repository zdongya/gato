<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.caiyi.financial.jzadmin.mapper.UserMapper">
	<!--mybatis ehcache缓存配置 -->
	<!-- 以下两个<cache>标签二选一,第一个可以输出日志,第二个不输出日志 <cache type="org.mybatis.caches.ehcache.LoggingEhcache" 
		/> -->
	<!-- <cache type="org.mybatis.caches.ehcache.EhcacheCache"/> -->
	<!-- 以下与实体类的中字段一致 -->
	<sql id="selectId">
		id,
		userName,
		accountName,
		t.rolename,
		password,
		description,
		locked,
		credentialsSalt,
		createTime
	</sql>

	<select id="findUserPage" resultType="com.caiyi.financial.jzadmin.entity.UserFormMap">
		select
		<include refid="selectId" />
		from ly_user u,(
		select  ur.userid,to_char(wm_concat(r.name)) rolename from ly_user_role ur,ly_role r where r.id=ur.roleid group by ur.userid) t 
		where t.userid=u.id
		<if test="accountName != null and accountName != ''">
		and accountName like '%${accountName}%'
		</if>
		<if test="column != null">
          order by ${column} ${sort}
        </if>
	</select>
	
</mapper>